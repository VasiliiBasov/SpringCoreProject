# 📋 Прогресс: контекст и навигация

Здесь — **только** то, что нужно для быстрого входа в работу: где остановились, что дальше, шпаргалка для следующего открытия.

**Цифры, время, баллы — в `STATS.md`.**
**Конспект теории — в `LEARNING_LOG.md`.**
**Краткое резюме (если история слетит) — в `HANDOFF.md`.**

---

## 📌 Где мы сейчас

**Курс завершён:** 28.08.2026 13:57
**Процент:** 100% ✅
**Всего кода в проекте:** ~12 файлов, Maven, Java 21, Spring 6.1.14 + spring-boot 3.2.5.

**Что сделано в последней сессии (28.08.2026 12:47–13:57):**
- ✅ Шаг 11 часть Б (события) — теория + мини-экзамен 60%
- ✅ Шаг 11 часть А (`@ConfigurationProperties`) — теория + мини-экзамен 93% + **рабочий эксперимент**:
  - Добавил `spring-boot:3.2.5` в `pom.xml`
  - Создал `EmailProperties.java` (`@ConfigurationProperties(prefix = "email")`)
  - Зарегистрировал через `@EnableConfigurationProperties(EmailProperties.class)` в `AppConfig`
  - Увидел в логе: `smtpPort=25` (дефолт из класса), `smtpHost=smtp.local` (из properties)
- ✅ Шаг 12 (Финал) — мини-экзамен 73% (BPP в жизн.цикле 4/5, Core vs Boot 2/5, AOP для метрик 5/5)

**Что освоено за курс (12 шагов):** DI, scope, BPP, lifecycle, AOP-прокси, SpEL, `@PropertySource`, события, `@ConfigurationProperties`.

**Что подтянуть перед собесом** (приоритет):
1. SpEL внутри `${}` — путаница с парсером
2. JDK Proxy vs CGLIB — правило выбора
3. Приоритет property sources — «последний = высший»

---

## 🔑 Шпаргалка для следующего открытия (ПРОЧИТАТЬ ПЕРВЫМ ДЕЛОМ)

**Если ученик написал «прочитай прогресс» — прочитай ОБЯЗАТЕЛЬНО все 4 файла в таком порядке:**

1. **`HANDOFF.md`** — краткое резюме всего курса. Если контекст потерян — это главный файл.
2. **`PROGRESS.md`** (этот файл) — где остановились, что дальше.
3. **`STATS.md`** — баллы, время, прогресс.
4. **`LEARNING_LOG.md`** — конспект теории, разборы шагов, копилка фактов.

### Суть
- **Ученик:** Василий. Готовится к собеседованию по Spring Core. Идёт 12-шаговым плану по проекту **Notification Hub** (Spring Core **без Spring Boot**).
- **Профиль:** ценит глубокие разборы с аналогиями, ASCII-схемы, мини-экзамены. **Не любит готовый код классов целиком** — давай только подсказки и направление.
- **Формат мини-экзаменов:** **по 1 вопросу за раз**, без вариантов ответа, своими словами (ученик попросил 26.08.2026).

### Главное правило работы с кодом
**Самый актуальный код — в файлах проекта, а не в чате.** Если ученик говорит «посмотри код» / «проверь» / «так можно?» — **сначала открыть файлы** (`Get-ChildItem` + `read_files`), не просить вставить код в чат. Подробности — в `LEARNING_LOG.md`, раздел «🛠 Правило работы с кодом в проекте».

### Главное правило про время
**Не угадывай время — всегда `Get-Date`.** Если ученик говорит про таймер («пауза», «продолжаю», «сколько уже»), сначала вызови `Get-Date -Format "dd.MM.yyyy HH:mm:ss"`. Активное время = время БЕЗ пауз. Подробности с примерами ошибок — в `STATS.md`, раздел «Примеры ошибок из 25.08.2026 (НЕ повторять)».

---

## 🎯 Следующие действия

1. ✅ **Разбор DI** — сделан, балл поднят 🔴 30% → 🟢 85%
2. ✅ **Шаг 3:** SmsChannel + цепочка через `getBeansOfType` — сделан
3. ✅ **Мини-экзамен** по шагам 2–3 — сделан (3 из 6 верно, выявлены 3 новых пробела)
4. ✅ **Шаг 4 (Component scan):** сделан — `@Component` на каналах, `AppConfig` стал «тонким», `@Value` на параметре конструктора
5. ✅ **Шаг 5 (BeanPostProcessor):** сделан — `LoggingBeanPostProcessor`, разбор порядка регистрации BPP
6. ✅ **Шаг 6 (Bean lifecycle):** сделан — `@PostConstruct`/`@PreDestroy` добавлены, мини-экзамен 64%
7. ✅ **Шаг 7 (Bean scope):** сделан — эксперимент с `@Scope("prototype")` показал, что `@PreDestroy` не работает для prototype (Spring теряет ссылку)
8. ✅ **Шаг 8 (AOP):** сделан — создал `TimingBeanPostProcessor` через `ProxyFactory`, увидел `[Timing]` в логе. Открытие: Spring выбирает JDK Dynamic Proxy при наличии интерфейса. Теория `final` + CGLIB работает, но не применилась из-за выбора JDK
9. ✅ **Шаг 9 (SpEL/@Value):** сделан — мини-экзамен 87%, эксперимент с `EmailConfig` (SpEL-математика, дефолт в `${}`, тернарник через Java-метод). Открытие: SpEL **не умеет** читать properties через `${}` внутри `#{}` — только путь к полям бина. После фикса опечатки `{${email.smtp-host}` → `${email.smtp-host}` всё заработало (isLocal: true)